apiVersion: v1
kind: ResourceQuota
metadata:
  name: namespace-limits
  namespace: ignisai-production
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 8Gi
    limits.cpu: "10"
    limits.memory: 16Gi
    pods: "20"
    services: "10"
    persistentvolumeclaims: "5"
